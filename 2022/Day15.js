const helpers = require('./../helpers')
const input = helpers.fetchInput('2022', 'Day15', '\n')

const mapBeacons = (input, y) => {
    const sensors = []
    console.log('input', input)
    for (let i = 0; i < input.length; i++) {

    }
}

mapBeacons(input, 10)